numpy>=1.24,<2.1
opencv-python-headless>=4.8,<5
librosa>=0.10,<0.11
soundfile>=0.12
faster-whisper>=1.0,<2
# Optional: pedalboard improves parametric FX (ffmpeg filters used as fallback)
pedalboard>=0.9.0
# Optional heavy models (install manually if desired):
# pip install torch torchaudio pyannote.audio speechbrain deepfilternet
