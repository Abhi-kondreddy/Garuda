# Neural voice separation (recommended for optimum Voices) — ~2–4+ GB downloads
# Install AFTER core requirements.txt:
#   pip install -r requirements-voices.txt
#
# App: Settings → Download voice models ON → Voices → Re-detect
# Full checklist (tiers + pyannote + settings): ../../SETUP.md
#
# Optimum diarization (gated HF models) — install separately:
#   pip install pyannote.audio
#   https://huggingface.co/pyannote/speaker-diarization-3.1

torch
torchaudio
speechbrain
# Optional — better speaker labels:
# pyannote.audio
